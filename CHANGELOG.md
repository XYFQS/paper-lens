# 更新记录

本文件记录各个版本的用户可见变化。安装包版本号取自 `addon/manifest.json`。

## 2.0.0

关键词从三类扩展为六类，并新增文献身份证、人工校正和按维度检索。旧版本的索引会在首次读取时自动升级，不会丢失已有关键词。

### 新增

- **六类中英关键词**：主题、区域、对象、方法、数据、变量，每类最多 5 个，中英文成对生成。信息不足时该类为空，不猜测、不凑数。
- **主要结论**：从标题、摘要或明确的元数据中提取，最多 3 条，作为记忆句和搜索的依据。
- **文献身份证**：搜索页内显示当前选中论文的完整画像，包括记忆句、研究画像、主要结论和文献管理字段。默认是一张只有文字的信息卡片，点右上角「编辑」才展开控件。不再是独立的第四个页面。
- **记忆句**：自动生成 `作者 年份 | 期刊 | 区域 | 方法 | 结论`，缺少的字段会省略。可以手动改写，也可以一键恢复自动生成。生成记忆句不会额外调用 AI。
- **阅读进度与用途**：阅读状态（未读 / 浏览过 / 已读 / 已掌握）、重要程度（普通 / 重要 / 核心）、在论文中的用途（Introduction / Methods / Results / Discussion / Data / Study Area / Review）。这些字段完全由你设置，AI 不会判断。
- **人工校正关键词**：可以停用某个 AI 关键词，也可以补一个自己的中英配对。停用会记录为排除项，AI 重新生成后该词不再出现；自己添加的词不会被 AI 结果覆盖。六个类别共用同一套添加控件；出现人工改动后才会显示「恢复 AI 原始画像」，一次清除全部改动。
- **十二个检索维度**：条目类型、六类关键词、年份、期刊、阅读进度、重要程度、用途，全部支持“包含 / 不包含”，并可组合“全部满足 / 任一满足”。
- **英文规范原生标签**：`T::`、`R::`、`O::`、`M::`、`D::`、`V::` 前缀，写入为 Zotero 自动标签，只保留一套英文命名。

### 变化

- **文献身份证改为默认信息卡片**：日常视图里没有任何输入框、删除按钮和维护动作，只有点「编辑」才展开控件，点「完成」收起，切换到另一篇论文会自动回到卡片。信息顺序固定为论文基本信息 → 记忆句 → 研究画像 → 主要结论 → 文献管理。
- 顶部页面改为 **找文献 / 文库 / 设置** 三个；“准备文库”并入 **文库**。
- 原生标签改为纯英文规范形式（例如 `R::China`），不再写入中文标签。
- 新增原生标签的旧格式（`研究区域：中国`、`Study region: China`）在首次启动时自动替换为规范形式；只处理插件自己创建的标签。

### 升级与数据

- 首次读取旧索引时自动升级为 v2 结构，并在同一目录写入 `index-v1-backup.json` 备份；单独备份只写一次，不会覆盖更早的副本。
- 旧关键词按下述对应关系保留：研究区域 → 区域，研究对象 → 对象，研究方法 → 方法；主题、数据、变量三类为空。
- 升级后的关键词标记为“旧版关键词”，在“文库”中单独计数，并提示重新生成。文献信息、标签归属记录和人工标签都会保留。
- 索引无法读取时不会用空数据库覆盖旧文件，而是另行保留副本并提示重建缓存。

### 修复

- 修复文献身份证中的下拉框和用途标签：选择后偶发回退到原值，看起来像“改了没生效”。
- 修复主要结论的英文被挤成一两个字母一行：英文行原本落进了序号列，现在与中文一样占满正文宽度。
- 修复在主列表单击论文时文献身份证不跟随：此前依赖的条目通知不会在选择变化时发出，改为监听主列表自身的选中事件，单击与键盘切换都能立即更新。
- 修复折叠 Zotero 原生条目栏时文献透镜被一起隐藏：两个面板的折叠状态现已完全独立，各自的按钮只控制自己。
- 修复会议论文和书籍章节的期刊字段：`proceedingsTitle` / `bookTitle` 现在也会作为期刊参与记忆句和检索条件。

## 1.0.2

- 修复阅读器（PDF）标签页中收起 Zotero 原生侧栏时插件面板残留的问题，现在会随侧栏一起隐藏并恢复。

## 1.0.1

- 修复文库标签页中收起原生条目面板时插件面板残留的问题。

## 1.0.0

- 首个版本：本地读取与搜索文献元数据、AI 生成中英双语研究区域 / 研究对象 / 研究方法关键词、条件检索、可选同步 Zotero 原生双语标签。
